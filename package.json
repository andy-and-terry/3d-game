{
  "name": "3d-game",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "main": "src/main.js",
  "scripts": {
    "node:check-lts": "node ./tools/check-node-lts.js",
    "start": "npm run node:check-lts && electron ./src/main.js",
    "start:dev": "npm run node:check-lts && electron ./src/main.js",
    "build:win": "npm run node:check-lts && electron-builder --win nsis",
    "test": "node --test"
  },
  "devDependencies": {
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.andyandterry.game",
    "productName": "3D Game",
    "directories": {
      "output": "dist",
      "buildResources": "assets"
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "data/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "include": "tools/installer/include.nsh"
    }
  }
}
